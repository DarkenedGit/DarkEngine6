#pragma once

namespace Dark
{

    struct GtaoSettings
    {
        bool  enabled   = false;
        float radius    = 0.5f;
        float power     = 1.5f;
        float intensity = 1.0f;
    };

} // namespace Dark
